{x = y + z * m,
DOUBLE t1; t; a,
IF (a-b > c+l)
    {t = 3.0 *  5.0,
    t1 = t + 6.0,}
ELSE
    a = b-c,
END_IF,

DO {a = a * 2.0,
    x = a,}
UNTIL (x > 10_us),

PRINT # cadena de caracteres #,

VOID funcion1 (DOUBLE y){
    x = 3.0,
    RETURN,
},

funcion1(x),

a.funcion1(2.0),

}